<div id="<%= dom_id borrower_record %>">
  <p>
    <strong>Book:</strong>
    <%= borrower_record.book.title %>
  </p>

  <p>
    <strong>Borrowed at:</strong>
    <%= borrower_record.created_at %>
  </p>

  <%= link_to('return', borrower_record_return_path(borrower_record.id)) %>
</div>
